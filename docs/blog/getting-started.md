# Start here: editing this website

This sample post shows the structure of an article. You can delete it after writing your first post.

## A section

Write in Markdown. Use `##` for section headings, `**bold**` for emphasis, and `[links](https://example.com)` for references.

```python
print("Hello, world!")
```

For equations, use MathJax: $E = mc^2$.

Copy `templates/post-template.md` in the repository to `docs/blog/your-post.md`, edit its contents, then add the post to `mkdocs.yml` and the list on `docs/blog/index.md`.
